const root = 'individual-legal-aid'

const pages = {
  individualLegalAid: {
    slug: root,
    dashboard: {
      path: `${root}/dashboard`,
      slug: 'dashboard',
    },
    form: {
      path: `${root}/form`,
      slug: 'form',
    },
  },
}

export {pages}
