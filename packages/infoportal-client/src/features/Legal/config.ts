const root = 'individual-legal-aid'

const pages = {
  individualLegalAid: {
    slug: root,
    dashboard: {
      path: `${root}/dashboard`,
      slug: 'dashboard',
    },
    data: {
      path: `${root}/data`,
      slug: 'data',
    },
  },
}

export {pages}
